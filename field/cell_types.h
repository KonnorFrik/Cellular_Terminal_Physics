#ifndef __CELL_TYPES_H__
#define __CELL_TYPES_H__

#define NONE_SYMB " "
#define WATER_SYMB "~"

enum { // types
    NONE = 0,
    WATER,
};

enum { // statuses
    NOT_UPDATED = 0,
    UPDATED,
};

#endif
